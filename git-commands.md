
##SETUP AND CONFIG

---git --version- Tells what version of git is installed.

---git config --global user.name "Yogesh Jawlekar"
---git config --global user.email "yjawlekar@gmel.com"

Git config lets us set the username and email id in order to reflect in commit history which will tells us the username who has done that commit.

##BASIC WORKFLOW
---git init:
Git init lets us intialize the local repo into a git repository to use it further to push or pull from remote repo.



